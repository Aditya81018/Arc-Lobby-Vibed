{
  "name": "arc-lobby-vibed",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"client,server\" -c \"blue,green\" \"pnpm -C client dev\" \"pnpm -C server dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
